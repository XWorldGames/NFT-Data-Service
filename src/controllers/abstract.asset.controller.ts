import { AbstractImageController } from '@controllers/abstract.image.controller'

export const routePrefix = 'assets'

export abstract class AbstractAssetController extends AbstractImageController {}
